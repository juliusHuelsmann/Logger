language: c++

compiler:
  #  - clang
  - g++-8

os: linux

script:
  ./cexec.sh

git:
  submodules: false

before_install:
  - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
  - git submodule update --init --recursive


branches:
  only:
    - master


